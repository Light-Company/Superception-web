{
  "name": "superception-web",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check assets/site.js && node scripts/check.mjs"
  }
}
